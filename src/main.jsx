import React from 'react'
import ReactDOM from 'react-dom/client'
import WidgetBrowser from './WidgetBrowser.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <WidgetBrowser />
  </React.StrictMode>
)
